#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, then in uppercase.
 *
 * Return: 0
 */
int main(void)
{
	char c = 'a';
	char i = 'A';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	while (d <= 'Z')
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);

}
