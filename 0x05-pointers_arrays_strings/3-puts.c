#include "main.h"
#include <stdio.h>

/**
 * main - prints a string, followed by a new line, to stdout
 *
 * return: always 0.
 */
void _puts(char *str)
{
	int i;

	i = 0;
	while (str[i] != '\0')
		i++;
}
